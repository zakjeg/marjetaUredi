<Application x:Class="marjetaUredi.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:marjetaUredi"
             xmlns:viewmodels="clr-namespace:marjetaUredi.ViewModels"
             xmlns:views="clr-namespace:marjetaUredi.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:UserInfoViewModel}">
            <views:UserInfoView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:UserListViewModel}">
            <views:UserListView/>
        </DataTemplate>

    </Application.Resources>
</Application>
